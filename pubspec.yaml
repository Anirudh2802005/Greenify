name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Compatible Firebase + Google Sign-In versions
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  google_sign_in: ^5.4.2

  # ✅ Charts, Fonts, URL launcher
  fl_chart: ^1.1.1
  google_fonts: ^6.2.1
  url_launcher: ^6.3.1
  image_picker: ^1.0.4
  pdf: ^3.11.0
  path_provider: ^2.1.2
  shared_preferences: ^2.5.3
  firebase_storage: ^13.0.3
  cloud_firestore: ^6.0.3

dev_dependencies:
  flutter_launcher_icons: ^0.13.1



flutter:
  uses-material-design: true

  assets:
    - assets/icon.jpg
